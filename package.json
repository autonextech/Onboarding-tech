{
  "name": "onboarding-tech-root",
  "version": "1.0.0",
  "scripts": {
    "install": "cd backend && npm install",
    "build": "cd backend && npx prisma generate && npm run build",
    "start": "cd backend && node dist/index.js"
  }
}
